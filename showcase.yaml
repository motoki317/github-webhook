type: runtime

entrypoint: ./main
startup: |
  PATH=$PATH:$GOPATH/bin
  NS=$GOPATH/src/git.trapti.tech/toki
  REPO=$NS/github-webhook
  mkdir -p $NS
  ln -s `pwd`/ $REPO
  cd $REPO
  dep ensure -v
  make
  cd client
  yarn
  source ./env.sh

http_proxy: 10900
